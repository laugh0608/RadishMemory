# 阶段 1 本地资料库质量验收计划

状态：计划，尚未完成对应实现与验收。依据：[2026-09-05 审阅](../implementation/2026-09-05-project-review.md)。本文件补充 production 入口和用户体验证据，不替换 M0 fixture、`P1-F01` 至 `P1-F18`、`P1-HF01` 至 `P1-HF12` 或 Source Vault 验收，不新增 canonical schema、fixture operation 或另一套 application API。

具体批次顺位、停止线与授权范围以[当前状态](../status/current.md)为准。Q01 至 Q08 是后续工作引用的验收编号，不是已经启用的 CI gate；实现与指标阈值应随对应修复或设计评审确定。

## 入口与数据

使用纯合成 `.txt` / `.md`、独立临时资料库、固定生成 seed 和显式授权根。业务操作通过 production application service；涉及 controller、UI 可到达性或系统 picker 时追加对应宿主验证。需要损坏注入时，只操作任务专用合成数据库，不访问用户资料库。

测试 oracle 必须与实现输出分离。不得将预期命中塞入 production query，不得只通过 runner 专用词项扩展或内存历史投影宣称产品能力成立。文件重启、默认 production features 与 opt-in 测试 features 的证据分别记录。

## 场景与关闭条件

| ID | 场景 | 后续验收要求 |
| --- | --- | --- |
| Q01 | 中文及中英混合找回，对应 R01 | 覆盖中文词语、标点、两字词、英文缩写、混合文本、无答案及高相似干扰；使用人工标注来源集合和固定 top-k，比对 production 检索的命中与误召回；测试包含已复现句子但不限于该句 |
| Q02 | 完整目录访问，对应 R02 | 至少 201 个来源，覆盖翻页边界、稳定排序、末页、跨页搜索定位及更新 / 导出 / 删除；不要求来源先在当前目录页缓存中；同时间记录有确定排序 |
| Q03 | 启动失败后的派生维护，对应 R03 | 关闭并重开仅派生损坏的库后，可以从明确维护入口验证 canonical facts 并显式 rebuild；完成后重新验证并恢复正常入口；canonical / binding 损坏、失败修复与用户取消均保持关闭，不创建空库掩盖故障 |
| Q04 | 数据量与读取性能，对应 R04 | 在例如 100 / 1000 / 10000 来源档位、短文 / 长文、多版本与大量匹配查询上记录冷 / 热启动、目录、搜索、导入、rebuild 耗时和峰值内存；发布构建、机器、文件大小分布、样本数及 p50 / p95 口径固定后再决定预算，不把合成内存 runner 耗时当磁盘产品性能 |
| Q05 | 评测与产品语义一致，对应 R05 | 过滤顺序须有可观察拒绝证据，禁止以常量成功证明；历史查询关闭重开后仍走正式能力，区分 runner 编排与 product API；词项扩展如成为产品行为须在正式边界实现并覆盖安全 / 相关性回归 |
| Q06 | 回源与新旧结果，对应 R06 | 命中在正文第 400 字符之后仍能定位到实际版本与片段；更新后旧搜索结果明确失效或重新计算；删除后缓存结果不继续展示正文；UI 错误与用户能采取的动作对应 |
| Q07 | 加密保护范围，对应 R07 | 对象加密接入后分别检查对象目录、SQLite / FTS、迁移前明文、导出与备份边界；原始对象无法直接读取不等于全文副本已加密；缺 key、错误 key、损坏、取消和重启均按 ADR 0008 报告真实状态 |
| Q08 | 恢复与迁移，对应 R08 | 先明确已实现的单原件 exact export 与尚未设计的整体迁移 / 恢复；在方案授权后演练对应备份、密钥丢失 / 锁定、系统重装及恢复完整性；不能以复制数据库或密文文件宣称恢复全部记忆 |

Q01 至 Q06 优先作为现有文本产品的质量闭环；Q07 必须等待对象加密正式接入；Q08 的新恢复或整体迁移能力须先有受审阅契约，不能借本表扩大 ADR 0008 首批范围。

## 证据与指标

每次记录基线 commit、构建 features、平台、合成生成器 / seed、对象数量、大小和版本分布、实际入口、操作结果及未覆盖项。日志只保留最小计数、状态和耗时，不复制正文、路径、数据库或密钥。

检索质量除 Recall@k 外还应观察无答案误召回、高相似干扰、引用定位和更新后旧版本排除。性能指标先建立可重复基线，再由对应评审确定预算；优化不能省略权限拒绝、完整性、时间或删除验证。

后续记忆控制台增加候选接受 / 拒绝率、重复候选率、每次确认耗时及每周维护时间，用于衡量用户负担。当前未引入真实用户研究、telemetry、模型调用或外发数据采集。

关闭某个审阅项时，必须在当前状态关联实际修复提交和对应证据；没有实现、未运行、环境受阻和已运行失败应分别报告。文档、静态检查或健康库上的 rebuild 成功不能替代受损库启动后的维护入口验证。
