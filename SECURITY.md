# RadishMemory 安全与隐私报告策略

RadishMemory 面向长期个人资料、记忆、模型外发和多端同步。越权召回、错误隐私声明、记忆污染和不完整删除都可能产生长期影响，应优先私下处理。

## 私下报告

请不要为未修复漏洞创建公开 Issue、Discussion 或 Pull Request，也不要公开粘贴利用代码、真实个人资料、凭据、密钥、原始记忆库或生产日志。

优先使用 GitHub 仓库 `Security` 页面中的 Private vulnerability reporting。若该入口不可用，可发送邮件至 `laugh0608@foxmail.com`，主题包含 `[RadishMemory Security]`。

报告请尽量包含：

- 受影响的提交、组件、接口、信任模式或部署条件；
- 使用合成数据的最小复现、预期行为和实际行为；
- 对机密性、完整性、可用性、数据主权或删除保证的影响；
- 攻击前提、影响范围、已知缓解和建议披露时间；
- 是否可以公开致谢及希望使用的署名。

如果复现确实需要敏感材料，请先描述材料类型并等待安全传输安排。不要通过普通邮件、Issue 或 PR 发送真实密码、Token、私钥、恢复码、个人资料或未经授权取得的第三方数据。

## 安全问题范围

- namespace、设备、应用、来源、字段或用户级授权绕过；
- `local_only` 等外发策略被模型调用、摘要、Embedding、缓存或图投影绕过；
- 导入资料中的 Prompt Injection 被提升为系统指令、工具授权或 `Procedure`；
- 模型推断绕过 `MemoryProposal` 和确认流程，污染已确认记忆；
- 来源、时间、冲突、撤回或敏感度被错误归属并造成可利用结果；
- 日志、诊断包、审计、CI artifact 或错误响应泄露私密正文、路径、密钥或 `ContextPack`；
- 删除状态被错误标记为完成，或正文、索引、缓存、同步设备、服务端和备份仍可恢复；
- 设备身份、吊销、密钥包装、同步冲突或恢复流程导致未授权访问或数据回退；
- 解析器、OCR、转写、插件、模型 SDK、依赖、构建或发布链造成代码执行、隐式联网、供应链或资源耗尽风险。

普通拼写、无安全影响的文档错误、功能建议和不涉及信任边界的缺陷可以使用公开 Issue。不能确定时，优先私下报告。

## 支持范围与披露

项目当前处于预实现阶段，尚未承诺稳定版本、安全支持周期或响应 SLA。维护者按 best-effort 方式确认、复现、评估和协调修复；未经协调，不应在修复或缓解可用前披露可直接利用的细节。

安全修复仍应通过受保护分支的 Pull Request 流程进入 `master`，随后按仓库规则回流 `dev`。修复用 fixture 必须合成或脱敏，真实用户资料和秘密不得进入长期提交历史。
