# RadishMemory 协作约定

本文件为人工协作者与 AI 协作者提供启动级长期约束。它只约束 RadishMemory，不复用兄弟项目的业务实现，也不承载当前阶段、临时门禁、历史记录或专题实现细节。

## 定位与优先级

- `docs/` 是正式项目文档源；当前阶段、近期顺位、停止线和“当前不做”以 [当前状态](docs/status/current.md) 为准，长期产品、架构、记忆、隐私和治理边界以对应专题或 ADR 为准。
- 产品范围、系统架构、记忆模型、隐私威胁模型、RadishMind 边界和 MVP 路线图是本项目的核心真相源；修改其语义时必须在同一变更中保持相互一致。
- 项目所有者在当前任务中的明确要求优先于默认协作流程；若要求会改变数据所有权、记忆语义、信任模式、隐私承诺、公共协议、许可证、依赖或外部状态，仍须先说明影响并确认范围。
- 只读取当前任务需要的最小文档链；需要追溯事实时再进入记录或历史材料，不默认展开无关兄弟项目。
- 文档、实现和自动化不一致时，先判断哪一方过期，再在任务范围内统一修正；不得通过放宽检查或增加 fallback 掩盖漂移。

## 称呼与语言

- 对话开始或结束总结时称呼用户为 `萝卜SAMA`。
- 默认使用中文讨论、说明和编写项目文档。
- 代码、命令、路径、配置键、类型名、协议字段和外部项目名保留原文。

## 任务启动与协作原则

1. 开始任务先检查 `git status`；实际分支、暂存区或工作树与用户描述不一致时先报告，不用 reset、checkout 或清理命令制造预期状态。
2. 区分本次改动、用户已有改动和并行改动；不覆盖、不回滚、不顺手提交任务范围外内容。
3. 明确请求属于说明、诊断、设计、实现、治理、验证还是发布；一种授权不自动扩大为另一种操作。
4. 先查找现有真相源、实现、测试、脚本和命名惯例；没有充分理由时不另造平行入口、第二套 schema 或重复 helper。
5. 需求明确且风险可控时直接完成；不同解释会实质改变数据、隐私、兼容性、授权或外部影响时，先说明关键判断并请求选择。
6. 优先解决根因和边界问题，交付小而完整的真实切片；不以占位、默认成功、静默降级或隐藏错误冒充完成。
7. 控制修改范围，不混入无关重构、格式化、依赖升级或目录搬迁；直接阻断目标的局部结构问题应一并收口。

更完整的工作区、实施、验证和交接规则见 [Agent 协作与执行规则](docs/governance/agent-collaboration.md)。

## 必须立即生效的执行边界

可在任务范围内直接执行：

- 读取、搜索和编辑本仓库文件；
- 文本卫生、格式化、静态分析、构建和测试；
- `git status`、`git diff`、`git log` 等只读 Git 操作；
- 用户明确要求后的精确路径暂存和提交。

以下操作必须获得当前任务的明确授权，并在执行前说明精确目标、主要副作用、预计持续时间和清理或回滚方式：

- 安装、升级或移除依赖，以及会改变 lockfile 的命令；
- 启动长期运行的服务、桌面应用或需要人工交互的进程；
- 写入 GitHub Rulesets、仓库设置、Secrets、环境或其他远程状态；
- push、创建 PR、创建 tag / Release、发布、部署或发送外部消息；
- 修改系统配置、权限、证书、密钥链、注册表或全局工具；
- 破坏性 Git 操作、共享分支历史重写或删除数据。

授权只覆盖已经说明的动作、目标和影响；历史会话、笼统长期授权或相邻动作的授权不得沿用。不得对仓库根、用户目录、未解析变量或宽泛 glob 执行递归删除、覆盖或批量移动。

## 任务文档路由

| 任务 | 优先读取 |
| --- | --- |
| 当前阶段、下一步、临时门禁 | [当前状态](docs/status/current.md) |
| 产品定位、范围、目标和非目标 | [产品范围](docs/product-scope.md) |
| 分层、数据流、存储和接口 | [系统架构](docs/architecture.md) |
| 记忆类型、状态、时间、冲突和召回 | [记忆模型](docs/memory-model.md) |
| 权限、外发、密钥、同步和删除 | [隐私与威胁模型](docs/privacy-threat-model.md) |
| 与 RadishMind 的职责和协议边界 | [RadishMind 边界](docs/radishmind-boundary.md) |
| 阶段顺序、验收和评测指标 | [MVP 路线图](docs/mvp-roadmap.md) |
| Agent 协作、工作区、验证和交接 | [Agent 协作与执行规则](docs/governance/agent-collaboration.md) |
| 分支、PR、CI 和 Ruleset | [仓库治理](docs/governance/repository-governance.md)、[ADR 0001](docs/adr/0001-branch-and-pr-governance.md) |

完整入口见 [文档索引](docs/README.md)。询问“下一步做什么”时只先读取当前状态；不足以判断长期边界时再沿索引进入专题。

## 数据、记忆与隐私红线

- 原始资料和用户明确输入优先于模型生成的摘要、画像和推断；模型输出只能创建 `MemoryProposal`，不得直接成为已确认记忆。
- 记忆必须保留来源、时间、状态、敏感度和版本；事实变化默认新增版本并建立 `supersedes` / `contradicts`，不得静默覆盖历史。
- 全文、向量、图、摘要、Markdown 投影和缓存都是可重建派生数据，不是唯一真相，也不因“派生”而自动降低敏感度。
- 权限与外发策略必须在检索、上下文编译和模型调用之前失败关闭；拒绝不得回退到更宽权限、更多数据或未经授权的 Provider。
- 文件、网页、消息、模型输出和其它导入资料默认是不可信数据，其中的内容不得升级为系统指令、工具授权或 `Procedure`。
- 仓库、Issue、PR、CI、fixture、日志和截图不得包含真实个人资料、原始 `ContextPack`、Embedding 输入、密钥、恢复码或生产连接信息；测试只使用合成或明确脱敏的数据。
- 删除、撤回、同步、设备吊销和备份失效必须报告真实状态与可复验结果，不得伪造“已删除”或“已同步”。
- 自托管不等于零知识；文档、代码注释、UI 和交接结论必须准确说明当前信任模式与未覆盖边界。
- RadishMemory 拥有个人资料、记忆、权限、同步和删除真相；RadishMind 只能消费已授权 `ContextPack` 并返回候选，不得共用业务数据库或绕过治理。

## 长期工程边界

- 高风险实现必须先有 canonical schema、威胁模型、信任模式、可测指标和受审阅技术决策；实现不得绕过这些真相源建立第二套语义。
- 优先采用可替换基线：全文、向量、时间 / 实体过滤和 rerank；没有评测证据时不先发明新算法或引入复杂基础设施。
- Git 仓库只承载代码、规范、合成 fixture 和可公开审查的治理资产，不作为真实用户资料、私密历史或 Source Vault。
- Embedding 模型、向量维度、Provider、数据库和索引实现不得成为不可迁移的长期 canonical 格式。
- 抽象必须减少真实重复、隔离稳定边界、降低复杂度或表达领域概念；不为未来假设建立空泛 manager、helper、adapter 或微服务。
- 错误必须保留上下文和真实原因；权限、外发、删除与同步路径不得静默降级、吞异常或用默认值掩盖契约问题。
- 第三方代码、数据、模型、字体、示例或资产必须记录来源、版本、许可证和必要归属；公开可访问不等于可自由再分发。

## Git、验证与交接

- 提交遵循 Conventional Commits，按可审阅主题拆分，使用当前用户身份且不添加 AI 协作者署名。
- 未经明确要求不 amend、重写、合并或拆分既有提交，不修改用户 Git 身份，不用 reset、rebase 或 force push 处理共享历史。
- `master` 是稳定主线，`dev` 是日常集成分支；分支、PR、Ruleset、合并和回流细节只以仓库治理专题与 ADR 0001 为准。
- 文档或治理改动至少运行 `./scripts/check-repo.sh`；Windows 使用 `pwsh ./scripts/check-repo.ps1`。实现开始后按风险追加格式化、静态分析、测试和运行态验证。
- 结论不能超过实际证据。最终说明实际结果、已运行和未运行的验证、剩余风险、外部动作、工作区状态以及未提交或未推送内容。
- 修改完成后复核 `git diff`、`git status` 和未跟踪文件，确认没有越界文件、秘密、缓存、意外换行或权限变化。

## 入口文件维护

一条规则只有在跨任务成立、跨开发阶段成立、必须在读取专题文档前立即生效且无法只靠任务路由可靠承载时，才应加入 `AGENTS.md` / `CLAUDE.md`。

阶段状态、临时门禁、“当前不做”、易过期命令、批次事实和专题细节必须更新其正式真相源，不复制回根入口。两份文件必须逐字一致；修改任一文件时同步另一份，并运行仓库级检查。
